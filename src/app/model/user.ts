export class User {
    username: string;
    totalPoints: number;

    constructor(username: string, totalPoints: number) {
        this.username=username;
        this.totalPoints=totalPoints;
    }
}